name: quasielasticbayes-dev-py310

channels:
    - conda-forge

dependencies:
    # A fortran compiler must be provided separately as we require a very specific version of mingw
    - python=3.10.*
    - numpy=1.21.*
    - pytest
